#ifndef UTILS_H
#define UTILS_H

#include <string>

/** Print a simple "done" message in green.
 *
 * @param message Optional message to print (default: "done\n")
 */
void print_done(const std::string message = "done\n");

#endif
